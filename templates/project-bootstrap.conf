# project-bootstrap configuration.
# This file is NON-SECRET and safe to version in your repository.
#
# Only topology/behavior overrides are allowed here. NEVER put credentials,
# tokens, DATABASE_URL, passwords, API keys or SSH private keys in this file.
#
# PROJECT_NAME and DEPLOY_NAME are DIFFERENT concepts:
#   PROJECT_NAME = name of the working repository (e.g. vaccine-fair-api)
#   DEPLOY_NAME  = name used for the homelab deploy (e.g. fair-principles-ic)
#
# Leave a value empty to accept the inferred/default value.

PROJECT_NAME=
DEPLOY_NAME=
DEPLOY_BRANCH=main
COMPOSE_BASE=compose.yaml
COMPOSE_DEV=compose.dev.yaml
COMPOSE_PROD=compose.prod.yaml
HOMELAB_ROOT=
REPOS_ROOT=
APPS_ROOT=
HOOKS_ROOT=
GLOBAL_PRE_RECEIVE=
DEPLOY_USER=
